{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug practice",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/practice",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake build (debug)"
        }
    ]
}
